@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    <div class="col-md-6">
        <h2>Velkommen @Model.Name</h2>
        <p>Oversigt over bestillinger</p>

        @if (Model.IsAdmin)
        {
            <p>Vi har 500 bestillinger, det sidste døgn</p>
        }
    </div>
</div>
